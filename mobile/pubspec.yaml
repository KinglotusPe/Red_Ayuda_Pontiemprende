name: red_ayuda
description: "Plataforma Tecnológica de Alerta y Coordinación Ciudadana ante Emergencias"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI e Iconos
  cupertino_icons: ^1.0.8

  # Networking & Realtime
  http: ^1.2.2

  # Estado y Utilidades
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
